"""Agents package initializer (keeps imports simple in tests).

This file is intentionally empty — it makes `agents` an explicit package
so imports like `from agents.hello import hello` resolve in more environments.
"""

__all__ = [
    "hello",
    "code_agent",
    "deploy_agent",
    "issue_agent",
]
